import java.util.Scanner;

public class Atletas {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        
        int atletas;
        
        
        
        
        // Pedir a quantidade de atletas
        System.out.print("Quantos atletas vai digitar ");
        atletas = sc.nextInt();
        
        for (int i = 1, i < n, i++) {
        	System.out.printf("Digite os dados do atleta" + i + ":");
        	
        	\\ digite o nome
        	System.out.print("Digite o nome: ");
        	string nome = sc.nextString();
        	
        	double porcentagemHomens = 0;
        	double porcentagemmulheres = 0;
        	
        	\\ digite o sexo
        	System.out.print("Sexo (F/M) :");
        	String sexo = sc.nextString();
        	while (sexo =! 'M' || =! 'F') {
        		System.out.printf("Sexo inválido" );
        		System.out.print("Sexo (F/M) :");
            	String sexo = sc.nextString();
        	}
        	if (sexo.equals('F')) {
        		System.out.printf("Feminino");
        		int qntMulheres = m++;
        	}
        	else(!sexo.equals('F')){
        		System.out.print("Masculino");
        		int qntHomens = h++;
        		
        	}
        	if (qntHomens > 0) {
                double porcentagemHomens = (qntHomens * 100.0) / n;
                
            } else {
                System.out.println("Porcentagem de homens: 0 %");
            }
        	
        	
        	\\ Digite a altura
        	System.out.print("Altura: ");
        	double altura = sc.nextDouble();
        	while (altura < 0) {
        		System.out.printf("Altura inválida, digite novamente" );
        		double altura = sc.nextDouble();
        		
        		
        		double alturaAtletaMaisAlto
        		if (altura > alturaAtletaMaisAlto) {
                    alturaAtletaMaisAlto = altura;
                    string nomeAtletaMaisAlto = nome;
                }
        	}
        	if (qntmulheres > 0) {
                double porcentagemmulheres = (qntmulheres * 100.0) / n;
                System.out.println("Porcentagem de Mulheres: " + porcentagemmulheres + " %");
            } else {
                System.out.println("Porcentagem de Mulheres: 0 %");
            }
        	
        	
        	double pesoTotal = peso + pesoTotal++;
        	\\Digite o peso
        	System.out.print("Peso: ");
        	double peso = sc.nextDouble():
        	While (peso < 0){
        		System.out.printf("Peso inválido, Digite novamente" );
        		double peso = sc.nextDouble():
        	}
        	
        	double pesoMedio = pesoTotal / atletas;
        	System.out.printf("Peso medio dos atletas: %.2f\n", pesoMedio);
        	System.out.println("Nome do atleta mais alto: " + nomeAtletaMaisAlto);
        	System.out.println("Porcentagem de homens: " + porcentagemHomens + " %");
        	System.out.println("Não há mulheres cadastradas");
        }
        
        scanner.close();
    }
}
